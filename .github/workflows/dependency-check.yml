name: Dependency validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lock-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Python 3.14
        uses: actions/setup-python@v7
        with:
          python-version: "3.14"

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry==1.8.4

      - name: Verify lockfile is tracked
        run: |
          if ! git ls-files poetry.lock | grep -q poetry.lock; then
            echo "ERROR: poetry.lock is not tracked by git"
            exit 1
          fi

      - name: Check lockfile consistency
        run: poetry check --lock

      - name: Verify Python version matches manifest
        run: |
          PYTHON_VERSION=$(python -c 'import sys; print(f"{sys.version_info.major}.{sys.version_info.minor}")')
          echo "CI Python version: $PYTHON_VERSION"
          # pyproject.toml declares python = "^3.14", so 3.14 is required
          if [ "$PYTHON_VERSION" != "3.14" ]; then
            echo "ERROR: CI must use Python 3.14, got $PYTHON_VERSION"
            exit 1
          fi
